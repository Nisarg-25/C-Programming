#include <stdio.h>

// Here arithmatic operator is used and all arithmaticc operators are binary.
// That means they want minimumm two operands.
int main(int argc, char const *argv[])
{
    int a, b;
    printf("Enter number a: ");
    scanf("%d", &a);
    printf("Enter number b: ");
    scanf("%d", &b);
    printf("Sum: %d\n" , a + b);
    return 0;
}

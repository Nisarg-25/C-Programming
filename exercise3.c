#include <stdio.h>

int main()
{
    printf("%d\n", printf("%s", "Hello World!")); // Here Hello World! will be printed because of %s and the number of digits will be printed because of %d.
    return 0;
}
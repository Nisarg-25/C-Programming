#include <stdio.h>

int main()
{
    char a = 7;
    a ^= 5;
    printf("%d" , printf("%d" , a += 3));
    return 0;
}

/* Here  the answer will be 51 because print function prints the value 5 with the number of characters.
   So the value of print function is 5 and total number of integers are 1 so answer is 51. */